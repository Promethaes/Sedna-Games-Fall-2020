using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RamThroughScript : MonoBehaviour
{
    //probably need to implement this when it comes down to this
    //just not sure how its gonna work. probably gonna be a fence or part of the level
    //that does a check that says ah is it the fuckin bison? then if they press the button SMASh

    public bool hasRammed = false;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    
}
